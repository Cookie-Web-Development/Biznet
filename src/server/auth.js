'use strict';

import passport from 'passport';
import bcrypt from 'bcrypt';
import LocalStrat from 'passport-local';
import mongoose from 'mongooose';

import { session_schema } from './schema/session_schema';

let auth = (app) => {

}

export default auth;